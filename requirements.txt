# Loose upper-bound floors — exact versions locked in requirements.lock.txt
# after `pip install -r requirements.txt && pip freeze > requirements.lock.txt`.

# Core data
pandas>=2.2,<3.0
numpy>=2.0,<3.0
pyarrow>=17.0
python-dateutil>=2.9
pyyaml>=6.0
requests>=2.32
tqdm>=4.66

# Modeling
scikit-learn>=1.5,<2.0
xgboost>=2.1,<3.0
lightgbm>=4.5,<5.0
lifelines>=0.29,<0.31
statsmodels>=0.14,<0.15

# Bayesian
pymc>=5.20,<6.0
numpyro>=0.16,<1.0
arviz>=0.20

# Plotting
matplotlib>=3.9,<4.0
seaborn>=0.13,<1.0

# Dev / test
pytest>=8.3,<9.0
pytest-cov>=6.0
ruff>=0.8

# Notebooks
ipykernel>=6.29
jupyterlab>=4.3

# LLM model (optional live path; offline heuristic fallback needs nothing)
anthropic>=0.40
